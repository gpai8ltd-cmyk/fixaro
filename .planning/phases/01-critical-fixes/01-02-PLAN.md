---
phase: 01-critical-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools-shop/src/components/Header.tsx
autonomous: false

must_haves:
  truths:
    - "User can see Instagram icon in header"
    - "User can click Instagram icon and navigate to Instagram profile"
    - "User can see Facebook icon in header"
    - "User can click Facebook icon and navigate to Facebook profile"
  artifacts:
    - path: "tools-shop/src/components/Header.tsx"
      provides: "Header with visible social media links"
      contains: "Instagram"
  key_links:
    - from: "tools-shop/src/components/Header.tsx"
      to: "https://instagram.com"
      via: "anchor tag href"
      pattern: "href.*instagram"
    - from: "tools-shop/src/components/Header.tsx"
      to: "https://facebook.com"
      via: "anchor tag href"
      pattern: "href.*facebook"
---

<objective>
Add social media links (Instagram, Facebook) to the site header for brand visibility.

Purpose: Improve brand presence and customer connection through visible social links.
Output: Header component with clickable Instagram and Facebook icons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tools-shop/src/components/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add social media links to header</name>
  <files>tools-shop/src/components/Header.tsx</files>
  <action>
Add Instagram and Facebook icons to the header, visible on desktop and mobile.

1. Import the icons (already available in lucide-react):
   Add to imports: `Instagram, Facebook` (from 'lucide-react')

2. Add social links in the "Right side actions" div (around line 150), BEFORE the search button:
```tsx
{/* Social links */}
<div className="hidden sm:flex items-center gap-1">
  <a
    href="https://instagram.com/toolsshop.bg"
    target="_blank"
    rel="noopener noreferrer"
    className="p-2 text-[var(--foreground)] hover:text-[var(--primary)] transition-colors"
    aria-label="Instagram"
  >
    <Instagram size={20} aria-hidden="true" />
  </a>
  <a
    href="https://facebook.com/toolsshop.bg"
    target="_blank"
    rel="noopener noreferrer"
    className="p-2 text-[var(--foreground)] hover:text-[var(--primary)] transition-colors"
    aria-label="Facebook"
  >
    <Facebook size={20} aria-hidden="true" />
  </a>
</div>
```

3. Add social links to mobile menu (in the bottom section with phone, around line 280):
```tsx
<div className="absolute bottom-0 left-0 right-0 p-4 border-t border-[var(--border)] bg-[var(--card)]">
  <a href="tel:+359888123456" className="flex items-center gap-2 text-[var(--foreground)] mb-3">
    <Phone size={18} className="text-[var(--primary)]" aria-hidden="true" />
    <span>+359 888 123 456</span>
  </a>
  <div className="flex items-center gap-3">
    <a
      href="https://instagram.com/toolsshop.bg"
      target="_blank"
      rel="noopener noreferrer"
      className="w-10 h-10 bg-[var(--card-hover)] rounded-lg flex items-center justify-center text-[var(--foreground)] hover:text-[var(--primary)] transition-colors"
      aria-label="Instagram"
    >
      <Instagram size={20} aria-hidden="true" />
    </a>
    <a
      href="https://facebook.com/toolsshop.bg"
      target="_blank"
      rel="noopener noreferrer"
      className="w-10 h-10 bg-[var(--card-hover)] rounded-lg flex items-center justify-center text-[var(--foreground)] hover:text-[var(--primary)] transition-colors"
      aria-label="Facebook"
    >
      <Facebook size={20} aria-hidden="true" />
    </a>
  </div>
</div>
```

Note: Using placeholder URLs (instagram.com/toolsshop.bg, facebook.com/toolsshop.bg). These should be updated with actual profile URLs when available.
  </action>
  <verify>
1. Header renders without errors
2. Desktop: Instagram and Facebook icons visible next to search
3. Mobile: Social icons visible in mobile menu footer
4. Icons have hover states (color change to primary)
5. Links open in new tab
  </verify>
  <done>
Header displays Instagram and Facebook icons on desktop and mobile. Icons are clickable and link to social profiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 Critical Fixes:
1. Fixed product search API (SQLite compatible)
2. Removed security badges from footer
3. Removed courier logos from footer
4. Removed 4.9/5 rating from hero section
5. Added Instagram and Facebook links to header
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd tools-shop && npm run dev`
2. Visit http://localhost:3000

**Header Check:**
- Look for Instagram and Facebook icons in the header (desktop view)
- Click hamburger menu on mobile - social icons should appear at bottom
- Click each social icon - should open in new tab

**Hero Section Check:**
- Hero shows "2 години гаранция" badge only
- No star rating or "4.9/5 от 500+ отзива" visible

**Footer Check:**
- Trust badges visible (Безплатна доставка, 2 години гаранция, Наложен платеж, Оригинални продукти)
- NO security badges section (SSL, Verified, 14-day, Data protection)
- NO courier logos (Econt, Speedy images)
- Copyright text visible at bottom

**Search Check:**
- Click search icon in header
- Type a search query (e.g., "бормашина" or a product name)
- Press Enter or click search button
- Products page should show results or "Няма намерени продукти" without errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full Phase 1 verification:
1. `cd tools-shop && npm run build` - No build errors
2. Homepage: Hero without rating, footer without badges/logos
3. Header: Social icons visible and clickable
4. Search: Returns results without API errors
</verification>

<success_criteria>
- NAV-01: Instagram link visible and clickable in header
- NAV-02: Facebook link visible and clickable in header
- All FIX requirements verified by human
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-fixes/01-02-SUMMARY.md`
</output>
