---
phase: 03-landing-animations
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tools-shop/src/components/WhyUsSection.tsx
  - tools-shop/src/components/TestimonialsSection.tsx
  - tools-shop/src/components/BrandPartners.tsx
  - tools-shop/src/app/(shop)/page.tsx
autonomous: true

must_haves:
  truths:
    - "Why Us section displays 3 brand story cards with icons"
    - "Testimonials section shows 3 customer reviews with star ratings"
    - "Brand partners strip shows tool brand logos"
    - "All sections fade in when scrolled into view"
  artifacts:
    - path: "tools-shop/src/components/WhyUsSection.tsx"
      provides: "Why choose us section with 3 cards"
      exports: ["default"]
    - path: "tools-shop/src/components/TestimonialsSection.tsx"
      provides: "Customer testimonials grid"
      exports: ["default"]
    - path: "tools-shop/src/components/BrandPartners.tsx"
      provides: "Brand logo strip"
      exports: ["default"]
  key_links:
    - from: "WhyUsSection.tsx"
      to: "AnimatedSection.tsx"
      via: "wrapper for scroll reveal"
      pattern: "AnimatedSection"
    - from: "TestimonialsSection.tsx"
      to: "AnimatedSection.tsx"
      via: "wrapper for scroll reveal"
      pattern: "AnimatedSection"
---

<objective>
Create trust-building landing page sections: Why Us, Testimonials, and Brand Partners.

Purpose: LAND-03 (Why Us), LAND-04 (Testimonials), and LAND-05 (Brand Partners) establish credibility and social proof. Users need to trust the shop before purchasing tools.

Output: Three new landing page sections that build customer confidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-landing-animations/03-01-SUMMARY.md
@.planning/phases/03-landing-animations/03-RESEARCH.md

# Files to reference/modify
@tools-shop/src/app/(shop)/page.tsx
@tools-shop/src/components/AnimatedSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhyUsSection component</name>
  <files>tools-shop/src/components/WhyUsSection.tsx</files>
  <action>
Create the "Why Choose Us" section with 3 brand story cards:

```typescript
// tools-shop/src/components/WhyUsSection.tsx
'use client';

import { Award, Truck, Headphones } from 'lucide-react';
import AnimatedSection from './AnimatedSection';

const reasons = [
  {
    icon: Award,
    title: 'Оригинални продукти',
    description: 'Работим директно с производителите. Всеки инструмент идва с пълна гаранция и сертификат за автентичност.',
    highlight: '100% оригинални',
  },
  {
    icon: Truck,
    title: 'Бърза доставка',
    description: 'Поръчките до 14:00 се изпращат същия ден. Безплатна доставка за поръчки над 100 лв. в цяла България.',
    highlight: 'До 24 часа',
  },
  {
    icon: Headphones,
    title: 'Експертна подкрепа',
    description: 'Нашият екип от специалисти ще ви помогне да изберете правилния инструмент за вашия проект.',
    highlight: 'Пон-Пет 9-18',
  },
];

export default function WhyUsSection() {
  return (
    <section className="py-16 md:py-20 bg-[var(--background)]" aria-labelledby="why-us-heading">
      <div className="container-custom">
        <AnimatedSection>
          <div className="text-center mb-12">
            <h2 id="why-us-heading" className="text-2xl md:text-3xl font-bold text-[var(--foreground)]">
              Защо да изберете нас?
            </h2>
            <p className="text-[var(--muted)] mt-2 max-w-2xl mx-auto">
              Повече от 10 години опит в продажбата на професионални инструменти
            </p>
          </div>
        </AnimatedSection>

        <div className="grid md:grid-cols-3 gap-6 md:gap-8">
          {reasons.map((reason, index) => (
            <AnimatedSection key={index} delay={index * 100}>
              <div className="bg-[var(--card)] rounded-xl p-6 md:p-8 border border-[var(--border)] card-hover-enhanced h-full">
                <div className="w-14 h-14 bg-[var(--primary)]/10 rounded-xl flex items-center justify-center mb-5">
                  <reason.icon className="text-[var(--primary)]" size={28} aria-hidden="true" />
                </div>
                <span className="inline-block px-3 py-1 bg-[var(--primary)]/10 text-[var(--primary)] text-sm font-medium rounded-full mb-3">
                  {reason.highlight}
                </span>
                <h3 className="text-xl font-bold text-[var(--foreground)] mb-3">
                  {reason.title}
                </h3>
                <p className="text-[var(--muted)] leading-relaxed">
                  {reason.description}
                </p>
              </div>
            </AnimatedSection>
          ))}
        </div>
      </div>
    </section>
  );
}
```

Key points:
- 3 cards: Оригинални продукти, Бърза доставка, Експертна подкрепа (as specified in requirements)
- Each card has icon, highlight badge, title, description
- Uses AnimatedSection with staggered delay (0, 100ms, 200ms)
- Uses card-hover-enhanced class from 03-01
- All Bulgarian text
- Accessible heading with id for aria-labelledby
  </action>
  <verify>
1. File exists: `ls tools-shop/src/components/WhyUsSection.tsx`
2. Has 3 reasons: `grep -c "icon:" tools-shop/src/components/WhyUsSection.tsx` (should be 3)
3. Uses AnimatedSection: `grep "AnimatedSection" tools-shop/src/components/WhyUsSection.tsx`
4. TypeScript valid: `cd tools-shop && npx tsc --noEmit`
  </verify>
  <done>
- WhyUsSection.tsx created with 3 cards
- Bulgarian labels matching requirements
- AnimatedSection used for scroll reveal
- Staggered animation delays
- card-hover-enhanced applied
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TestimonialsSection and BrandPartners components</name>
  <files>
    tools-shop/src/components/TestimonialsSection.tsx
    tools-shop/src/components/BrandPartners.tsx
  </files>
  <action>
Create TestimonialsSection with 3 customer reviews:

```typescript
// tools-shop/src/components/TestimonialsSection.tsx
'use client';

import { Star, Quote } from 'lucide-react';
import AnimatedSection from './AnimatedSection';

const testimonials = [
  {
    name: 'Георги Петров',
    role: 'Строителен предприемач',
    rating: 5,
    text: 'Използвам инструменти от този магазин вече 3 години. Качеството е безупречно, а доставката винаги е навреме. Препоръчвам!',
  },
  {
    name: 'Мария Иванова',
    role: 'Майстор мебелист',
    rating: 5,
    text: 'Намерих всичко необходимо за работилницата си. Отличен избор и конкурентни цени. Екипът беше много любезен при консултацията.',
  },
  {
    name: 'Димитър Стоянов',
    role: 'Домашен майстор',
    rating: 5,
    text: 'Като любител съм изненадан от професионалното обслужване. Помогнаха ми да избера правилния инструмент за моя проект.',
  },
];

export default function TestimonialsSection() {
  return (
    <section className="py-16 md:py-20 bg-[var(--card)]" aria-labelledby="testimonials-heading">
      <div className="container-custom">
        <AnimatedSection>
          <div className="text-center mb-12">
            <h2 id="testimonials-heading" className="text-2xl md:text-3xl font-bold text-[var(--foreground)]">
              Какво казват нашите клиенти
            </h2>
            <p className="text-[var(--muted)] mt-2">
              Над 5000 доволни клиенти ни се доверяват
            </p>
          </div>
        </AnimatedSection>

        <div className="grid md:grid-cols-3 gap-6 md:gap-8">
          {testimonials.map((testimonial, index) => (
            <AnimatedSection key={index} delay={index * 100}>
              <div className="bg-[var(--background)] rounded-xl p-6 border border-[var(--border)] card-hover-enhanced h-full relative">
                {/* Quote icon */}
                <Quote
                  size={32}
                  className="absolute top-4 right-4 text-[var(--primary)]/20"
                  aria-hidden="true"
                />

                {/* Star rating */}
                <div className="flex gap-1 mb-4">
                  {[...Array(5)].map((_, i) => (
                    <Star
                      key={i}
                      size={18}
                      className={i < testimonial.rating ? 'fill-[var(--primary)] text-[var(--primary)]' : 'text-gray-300'}
                      aria-hidden="true"
                    />
                  ))}
                </div>

                {/* Quote text */}
                <p className="text-[var(--foreground)] mb-6 leading-relaxed">
                  "{testimonial.text}"
                </p>

                {/* Author */}
                <div className="flex items-center gap-3">
                  <div className="w-12 h-12 rounded-full bg-gradient-primary flex items-center justify-center text-white font-bold text-lg">
                    {testimonial.name.charAt(0)}
                  </div>
                  <div>
                    <p className="font-semibold text-[var(--foreground)]">{testimonial.name}</p>
                    <p className="text-sm text-[var(--muted)]">{testimonial.role}</p>
                  </div>
                </div>
              </div>
            </AnimatedSection>
          ))}
        </div>
      </div>
    </section>
  );
}
```

Create BrandPartners component:

```typescript
// tools-shop/src/components/BrandPartners.tsx
'use client';

import AnimatedSection from './AnimatedSection';

// Using text logos since we don't have actual brand logo images
const brands = [
  { name: 'Bosch', color: '#E30613' },
  { name: 'Makita', color: '#009EDA' },
  { name: 'DeWalt', color: '#FEBD17' },
  { name: 'Milwaukee', color: '#DB021D' },
  { name: 'Metabo', color: '#00A651' },
  { name: 'Festool', color: '#009FE3' },
];

export default function BrandPartners() {
  return (
    <section className="py-12 border-y border-[var(--border)]" aria-label="Партньорски марки">
      <div className="container-custom">
        <AnimatedSection>
          <p className="text-center text-sm text-[var(--muted)] mb-6 uppercase tracking-wide">
            Доверени марки, които предлагаме
          </p>
          <div className="flex flex-wrap justify-center items-center gap-8 md:gap-12">
            {brands.map((brand, index) => (
              <div
                key={index}
                className="text-2xl md:text-3xl font-bold opacity-60 hover:opacity-100 transition-opacity cursor-default"
                style={{ color: brand.color }}
              >
                {brand.name}
              </div>
            ))}
          </div>
        </AnimatedSection>
      </div>
    </section>
  );
}
```

Key points:
- Testimonials: 3 Bulgarian customers with realistic names, roles, and feedback
- Star ratings using lucide Star icon with fill
- Quote icon as decorative element
- Avatar shows first letter of name with gradient background
- BrandPartners: 6 major tool brands (Bosch, Makita, DeWalt, Milwaukee, Metabo, Festool)
- Using text with brand colors since actual logos would require image files
- Hover effect increases opacity for interactivity
- Both use AnimatedSection for scroll reveal
  </action>
  <verify>
1. Files exist: `ls tools-shop/src/components/TestimonialsSection.tsx tools-shop/src/components/BrandPartners.tsx`
2. Has 3 testimonials: `grep -c "rating:" tools-shop/src/components/TestimonialsSection.tsx` (should be 3)
3. Has 6 brands: `grep -c "name:" tools-shop/src/components/BrandPartners.tsx` (should be 6)
4. TypeScript valid: `cd tools-shop && npx tsc --noEmit`
  </verify>
  <done>
- TestimonialsSection.tsx created with 3 Bulgarian customer reviews
- BrandPartners.tsx created with 6 tool brands
- Both use AnimatedSection for scroll reveal
- Star ratings work correctly
- card-hover-enhanced applied to testimonial cards
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate trust-building sections into landing page</name>
  <files>tools-shop/src/app/(shop)/page.tsx</files>
  <action>
Update the landing page to include the new sections in this order:
1. Hero (existing)
2. StatsBar (from 03-02)
3. Features (existing)
4. Categories (existing)
5. **WhyUsSection** (NEW - insert here)
6. Featured Products (existing)
7. **TestimonialsSection** (NEW - insert here)
8. **BrandPartners** (NEW - insert here)
9. Sale Banner (existing)
10. Newsletter/CTA (existing)

Add imports at top of file:
```typescript
import WhyUsSection from '@/components/WhyUsSection';
import TestimonialsSection from '@/components/TestimonialsSection';
import BrandPartners from '@/components/BrandPartners';
```

Insert WhyUsSection AFTER Categories section (after the closing </section> around line 250):
```tsx
      {/* Categories */}
      <section className="py-12 md:py-16" aria-labelledby="categories-heading">
        ...
      </section>

      {/* Why Us */}
      <WhyUsSection />

      {/* Featured Products */}
      <section className="py-12 md:py-16 bg-[var(--card)]" aria-labelledby="featured-heading">
```

Insert TestimonialsSection and BrandPartners AFTER Featured Products section (before Sale Banner):
```tsx
      {/* Featured Products */}
      <section className="py-12 md:py-16 bg-[var(--card)]" aria-labelledby="featured-heading">
        ...
      </section>

      {/* Testimonials */}
      <TestimonialsSection />

      {/* Brand Partners */}
      <BrandPartners />

      {/* Sale Banner */}
      <section className="py-12 md:py-16" aria-label="Промоция">
```

Key points:
- WhyUsSection goes after Categories (builds trust before showing products)
- TestimonialsSection after Featured Products (social proof after seeing products)
- BrandPartners after Testimonials (brand authority)
- Maintain existing section order, only add new sections
  </action>
  <verify>
1. Imports present: `grep -E "import.*(WhyUsSection|TestimonialsSection|BrandPartners)" tools-shop/src/app/\\(shop\\)/page.tsx`
2. Components used: `grep -E "<(WhyUsSection|TestimonialsSection|BrandPartners)" tools-shop/src/app/\\(shop\\)/page.tsx`
3. Build succeeds: `cd tools-shop && npm run build`
4. Dev server runs: Start `npm run dev` and verify all sections visible on homepage
  </verify>
  <done>
- WhyUsSection imported and placed after Categories
- TestimonialsSection imported and placed after Featured Products
- BrandPartners imported and placed after Testimonials
- Page builds without errors
- All sections visible and fade in on scroll
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` - passes without errors
2. Homepage shows "Why Us" section with 3 cards between Categories and Featured Products
3. Homepage shows Testimonials section with 3 reviews after Featured Products
4. Homepage shows Brand Partners strip with 6 brand names after Testimonials
5. Scroll through page - each section fades in when entering viewport
6. Hover on cards - lift/scale effect works
</verification>

<success_criteria>
1. LAND-03: "Why Us" section displays with 3 brand story cards
2. LAND-04: Testimonials section shows 3 customer reviews with star ratings
3. LAND-05: Brand partners strip shows 6 tool brand names
4. All sections use AnimatedSection for scroll-reveal
5. Cards have hover effects (card-hover-enhanced)
6. All text is in Bulgarian
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-animations/03-03-SUMMARY.md`
</output>
